# Assignment 1 - SwiftTranslator Automation

This is the automation test suite for the SwiftTranslator website using Playwright.

## 📋 Prerequisites
- Node.js installed on your machine.
- VS Code (Recommended).

## 🚀 How to Install Dependencies
Open your terminal in the project folder and run:

```bash
npm install
npx playwright install